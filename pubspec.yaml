name: hocuspocus_dart
description: A pure Dart port of the Hocuspocus WebSocket provider for Yjs CRDT collaboration.
version: 1.0.1
homepage: https://github.com/jagtesh/hocuspocus-dart

# Pinned to hocuspocus JS v2.12.3 (vendor/hocuspocus)

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  web_socket_channel: ^3.0.0
  yjs_dart:
    path: ../yjs-dart

dev_dependencies:
  lints: ^4.0.0
  test: ^1.24.0
  stream_channel: ^2.1.0
